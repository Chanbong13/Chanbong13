{
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && npx tsx prisma/seed.ts; next build",
  "framework": "nextjs",
  "installCommand": "npm install && npm install next@latest eslint-config-next@latest"
}
